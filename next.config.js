/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    // imageSizes: [320, 768, 1280],
    deviceSizes: [320, 768, 1280],
    formats: ["image/webp"],
  },
};

module.exports = nextConfig;
